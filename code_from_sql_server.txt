USE Programminghero;   -- database select

CREATE TABLE Student(
	Roll CHAR(4) PRIMARY KEY,
    Name VARCHAR(50),
    Marks DOUBLE
);

DROP table Student;  --delete the table

INSERT INTO Student  -- insert data in table
(Roll, Name, Marks) values(1, "arfin", 90);

INSERT INTO Student
(Roll, Name) values(2, "rahim");

INSERT INTO Student  -- PRIMARY KEY te abar data push kora jay Name
(Roll, Name) values(2, "korim");

SET SQL_SAFE_UPDATES = 0;  -- safe mode off
UPDATE Student
SET Name = 'Arfin Al Eusuf'
WHERE Roll = 1;
SET SQL_SAFE_UPDATES = 1;  -- safe mode on kora

SET SQL_SAFE_UPDATES = 0;
DELETE FROM Student  -- delete row
where Roll = 1;
SET SQL_SAFE_UPDATES = 1;